export default function Home() {
  return (
    <main className="page">
      <section className="hero">
        <p className="eyebrow">npm workspace</p>
        <h1>Next.js is running inside your monorepo.</h1>
        <p className="copy">
          Start here by adding more apps in <code>apps/</code> or shared code in
          <code> packages/</code>.
        </p>
      </section>
    </main>
  );
}
